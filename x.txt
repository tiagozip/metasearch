can you build a nice little translation feature? available both as an instant answer with a nice widget and as a standalone page, like google translate, all really nice looking and well tested.

to handle translations, make the client POST /translate, with a body { text: string, targetLang: string } and return { translatedText: string }. maybe even integrate dictionary partially sometimes.

to execute actual translations, just do this in the server:

❯ curl 'https://translate-pa.googleapis.com/v1/translateHtml' \
        -H 'accept: */*' \
        -H 'accept-language: en-US,en;q=0.9' \
        -H 'cache-control: no-cache' \
        -H 'content-type: application/json+protobuf' \
        -H 'pragma: no-cache' \
        -H 'priority: u=1, i' \
        -H 'sec-ch-ua: "Not/A)Brand";v="99", "Chromium";v="148"' \
        -H 'sec-ch-ua-mobile: ?0' \
        -H 'sec-ch-ua-platform: "macOS"' \
        -H 'sec-fetch-dest: empty' \
        -H 'sec-fetch-mode: cors' \
        -H 'sec-fetch-site: cross-site' \
        -H 'sec-gpc: 1' \
        -H 'x-client-data: CMyXywE=' \
        -H 'x-goog-api-key: AIzaSyATBXajvzQLTDHEQbcpq0Ihe0vWDHmO520' \
        --data-raw '[[["The zoning committee'\''s draft amendment would grandfather existing non-conforming structures, provided they undergo a seismic retrofit before the next fiscal quarter, unless the property falls within a designated heritage overlay district."],"auto","pt-PT"],"te_lib"]'

[["A proposta de alteração do comité de zoneamento isentaria as estruturas não conformes existentes, desde que passem por uma reforma sísmica antes do próximo trimestre fiscal, a menos que a propriedade esteja localizada num distrito de património histórico designado."],["en"]]⏎

for flags, use /Users/tiago/Desktop/code/security/cap/standalone/public/assets/flags.

make the ui genuinely good and clean. test with playwright if needed.

allow for document (pdf, docx) translation, basic text translation, and maybe even image? translation.